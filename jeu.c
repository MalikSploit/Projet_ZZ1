#include "jeu.h"

/* renvoie le score. Simule le jeu et run une de ces instance */
int Jeu(bot){

  /* SDL */
  /* iterJeu(grille, proie, chasseur, deplacement) */

  /* for (i=0; i < MAX_ITER; i++) { */
/* iterJeu(grille, proie, chasseur, deplacement) */
/* } */

}

bool iterJeu(int grille[], int * proie, int * chasseur, int deplacement){

  /* SDL */
  /* iterJeu(grille, proie, chasseur, deplacement) */

  /* for (i=0; i < MAX_ITER; i++) { */
/* iterJeu(grille, proie, chasseur, deplacement) */
/* } */

  /* test */

}
